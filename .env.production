# Point the frontend AI client at the serverless proxy (same origin, so no CORS).
# This file is committed and safe: it contains NO secrets — only a relative path.
# The actual API key lives in Vercel env vars as ANTHROPIC_API_KEY (server-side).
VITE_AI_ENDPOINT=/api/messages
